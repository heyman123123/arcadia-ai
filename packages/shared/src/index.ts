/**
 * @arcadia/shared - 前后端共享类型/常量/契约
 *
 * 使用方式:
 *   import type { BookProject, Character, StyleFilters } from '@arcadia/shared';
 *   import { WRITING_PROMPT_PRESETS, API_ROUTES } from '@arcadia/shared';
 */

export * from './types';
export * from './constants';
export * from './api';
